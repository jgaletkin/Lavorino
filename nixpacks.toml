[variables]
NODE_ENV = "development"
PORT = "3000"

[phases.setup]
providers = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = [
  "cd /app",
  "npm ci"
]

[phases.build]
cmds = [
  "cd /app", 
  "npm run build"
]

[start]
cmd = "cd /app && npm start"